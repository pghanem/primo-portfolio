/** @jsxImportSource @emotion/react */
import React from 'react'

import * as styles from './SocialLinks.styles';

const SocialLinks = (): React.ReactElement => {
    return <div css={styles.linksContainer}>

    </div>
}

export default SocialLinks;