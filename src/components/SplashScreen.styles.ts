import { css } from "@emotion/react";

export const wrapperStyles = css`
    position: fixed;
    inset: 0;
    display: grid;
    place-items: center;
`;

export const svgStyles = css`
    width: 150px;
    height: 150px;
`;
